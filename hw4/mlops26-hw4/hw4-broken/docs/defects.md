# Разбор четырёх дефектов

Симптомы были описаны в `README.md`, места отмечены `TODO`. Ниже по каждому
дефекту: в чём состоял, как проявлялся, как исправлен и каким числом это
подтверждается.

Все числа сняты на моём датасете из ДЗ 3 (версия v2, выход стадии `split`):
`data/train.jsonl` — 28 879 диалогов, `data/val.jsonl` — 3 610. Это обращения
в поддержку на английском и немецком; ответ ассистента — JSON с полями
`queue`, `type`, `priority`, `tags` и текстом ответа клиенту. Токенизатор —
`Qwen/Qwen3-1.7B`, `enable_thinking: false`. Воспроизводится командами
`make tokenize` и `make check`.

До исправлений `make check` проваливал 6 проверок из 9 (1, 2, 3, 4, 5, 8).
После — все 9 зелёные, отпечаток `tests/check.sh` не менялся: `f2002c8c9519`.

---

## 1. В лосс шёл весь текст, а не только ответ

### В чём состоял

Два места в `src/tokenize_data.py`, оба у `TODO`.

- `mask_prompt()` возвращала `list(input_ids)`: `labels` совпадали с
  `input_ids`, ни одна позиция не получала `-100`.
- В `encode_example()` граница промпта была заглушкой `n_prompt = 0`.
  Функция `prompt_token_len()` из `src/prompt.py`, которая эту границу
  считает, нигде не вызывалась. Даже работающая `mask_prompt()` не
  замаскировала бы ничего.

### Как проявлялся

Модель учится предсказывать системный промпт и текст обращения наравне
с ответом, то есть продолжать вопрос вместо ответа на него. Лосс при этом
ниже и падает быстрее: промпт длиннее, шаблоннее и проще для предсказания.

- Сломанная стадия печатала `токенов 6339773 (в лосс идёт 6339773)` — 100%.
- Проверка 1 на первом же примере декодировала под маской
  `'system\nProcess this support request as a helpdesk system would…'`
  вместо `'{"queue": "Returns and Exchanges", …'`.

### Как исправлен

- `encode_example()` строит строку инференса тем же `build_chat_text(...,
  add_generation_prompt=True)`, токенизирует полный текст с
  `return_offsets_mapping=True` и берёт границу из `prompt_token_len()`:
  сначала сравнение токенов промпта с началом полного текста, при склейке
  BPE — символьные офсеты.
- Перед этим стадия проверяет, что промпт — префикс полного текста, и падает,
  если нет: граница по символам без этого инварианта бессмысленна.
- `mask_prompt()` ставит `-100` на первые `n_prompt` позиций. `input_ids`
  остаются полными: модель читает промпт, но не штрафуется за него.
- В лосс по-прежнему идут ответ, `<|im_end|>` (eos) и перевод строки после него:
  без eos модель не научится останавливаться.

### Чем подтверждается

| train, `max_seq_len` 512 | токенов | доля |
|---|---:|---:|
| всего | 8 119 427 | 100% |
| под лоссом (ответ + eos) | 3 670 761 | 45.2% |
| промпт, `labels = -100` | 4 448 666 | 54.8% |

Без маски 54.8% обучающего сигнала уходило бы на переписывание вопроса.
В курсовом срезе эта доля 91%: там ответ — одна буква, а у меня JSON
со средней длиной 128 токенов. Промпт всё равно больше половины.

Проверка 1: под `label != -100` декодируется ровно ответ ассистента на всех
28 878 примерах train. Для обрезанных сверяется префикс ответа.

Запасной путь по офсетам на моих данных сработал 0 раз из 28 879: шаблон Qwen3
заканчивает промпт `\n\n`, BPE его с ответом не склеивает. Проверка 6 подтверждает,
что при склейке (промпт `«Приве»`, текст `«Привет, мир»`) граница уходит на
первый токен после промпта.

---

## 2. Шаблон обучения собирался руками и расходился с инференсом

### В чём состоял

В `src/prompt.py` путь инференса (`add_generation_prompt=True`) вызывал
`tokenizer.apply_chat_template`, а путь обучения собирал строку f-строкой
по константе `TURN = "<|im_start|>{role}\n{content}<|im_end|>\n"`,
«выписанной из карточки модели». Две копии одного формата, никем не сверенные.

### Как проявлялся

Шаблон Qwen3 при `enable_thinking=False` перед ответом ассистента подставляет
пустой блок `<think>\n\n</think>\n\n` — 4 токена: `151667, 271, 151668, 271`.
Ручная сборка его не знала. В итоге модель на обучении видит ответ сразу
после `<|im_start|>assistant\n`, а на инференсе ответ начинается после
think-блока. Метрики обучения этого не показывают, в проде это выглядит как
«дообучение не помогло».

- Строка инференса не была префиксом строки обучения ни в одном из
  3 610 диалогов val.
- Первое расхождение — байт 865 первого диалога: инференс продолжает
  `<think>`, обучение — `{"queue": …`.
- Других отличий нет: в 3 610 из 3 610 диалогов ручная строка совпадает с
  шаблонной, если из шаблонной убрать think-блок. На обучении модель
  недосчитывалась 4 токенов формата в каждом примере.
- Проверка 3 находила спецтокены ролей строкой в `src/prompt.py:9`.

### Как исправлен

`TURN` удалена. Оба пути `build_chat_text()` идут через один вызов
`apply_chat_template`: для инференса — промпт без последней реплики и
`add_generation_prompt=True`, для обучения — весь диалог и
`add_generation_prompt=False`, с одинаковыми `enable_thinking` из
`params.yaml`. `apply_chat_template` вызывается только в `src/prompt.py`,
спецтокенов ролей в `src/` нет.

`enable_thinking: false` оставлен сознательно. При `true` блок
`<think></think>` уходит из промпта в ответ и попадает в лосс, а побайтовая
проверка префикса этого не заметит. Этот случай ловит проверка 1: под маской
был бы не только ответ.

### Чем подтверждается

Проверка 2: строка inference-пути — побайтовый префикс строки train-пути на всех
3 610 диалогах val. Внутри стадии тот же инвариант проверяется для каждого
примера train и val. Проверка 3 зелёная по обоим пунктам.

---

## 3. Паддинг справа

### В чём состоял

Два места.

- `DynamicPaddingCollator` по умолчанию паддил справа (`padding_side="right"`).
- В `main()` у `TODO` параметр `tokenize.padding_side: "left"` из `params.yaml`
  так и не присваивался токенизатору. Стадия сохраняла в `data/tokenized/*.pt`
  и в `metrics/tokenize.json` дефолт токенизатора — `right`. Это значение и
  прочитали бы обучение и инференс.

### Как проявлялся

Decoder-only модель продолжает последний токен строки. При паддинге справа
у всех строк батча, кроме самой длинной, последними стоят pad-токены, и
генерация начинается после них, а не после промпта. На batch = 1 паддинга
нет, поэтому там всё в порядке.

Замер на промптах val (путь инференса), батчи по 8 в случайном порядке, тот же
коллатор:

| паддинг | строк, где последний токен — pad | pad между промптом и генерацией |
|---|---:|---:|
| справа | 3 154 из 3 610 (87.4%) | в среднем 103.5 токена |
| слева | 0 из 3 610 | 0 |

Проверка 4 падала на `в metrics/tokenize.json padding_side = 'right'`.

### Как исправлен

- В `main()` `tokenizer.padding_side = params["tokenize"]["padding_side"]`.
  Сторона паддинга теперь приходит из единственной точки конфигурации и в
  том же виде сохраняется в тензоры и метрики.
- Дефолт коллатора изменён на `"left"`.
- `labels` на паддинге — `-100`, `attention_mask` — 0: это в коллаторе было
  верно и осталось без изменений.

### Чем подтверждается

Проверка 4: `params.yaml`, `metrics/tokenize.json`, `data/tokenized/train.pt`
и коллатор по умолчанию — `left`. В собранном батче `attention_mask`
начинается с 0 и кончается 1, `labels` на паддинге -100.

Ширина батча на train (28 878 примеров, batch 8, полезных токенов 8 119 427):

| как паддим | токенов в батчах | из них pad |
|---|---:|---:|
| до `max_seq_len` 512 | 14 785 536 | 45.1% |
| динамически, случайный порядок | 12 041 252 | 32.6% |
| динамически, сортировка по длине | 8 120 832 | 0.02% |

Коллатор паддит до самого длинного примера в батче. Сортировка по длине
убирает почти весь паддинг, но ломает перемешивание, поэтому в ДЗ 5 её стоит
делать внутри буфера, а не по всему датасету.

---

## 4. Обрезка по `max_seq_len` не считалась, а резала больше половины

### В чём состоял

Тоже в два приёма.

- `truncation_stats()` возвращала `{}`. В метриках не было ни числа, ни доли
  обрезанных, порог `tokenize.truncated_warn_ratio` нигде не проверялся.
  Отчёт писал «Доля обрезанных НЕ ПОСЧИТАНА».
- `max_seq_len: 232` не соответствовал моему датасету. Медиана длины — 268
  токенов, то есть порог был ниже медианы.

### Как проявлялся

Обрезка идёт с конца и съедает ответ, а не вопрос. Стадия при этом молчала.

После того как стадия научилась считать обрезку, при `max_seq_len` 232:

| | train | val |
|---|---:|---:|
| обрезано примеров | 20 065 из 28 879 (69.5%) | 2 562 из 3 610 (71.0%) |
| ответ съеден целиком, пример выброшен | 3 083 | 449 |
| токенов ответа дошло до лосса | 2 039 927 из 3 702 438 | — |

Терялось 44.9% токенов ответа на train. Ответ — JSON, а у обрезанного
примера нет ни закрывающей скобки, ни eos: такой пример не учит ни закончить
JSON, ни остановиться.

### Как исправлен

1. `truncation_stats()` считает `truncated` и `truncated_ratio` по всем
   записям сплита, включая выброшенные. Если доля выше
   `truncated_warn_ratio`, стадия печатает предупреждение в stderr.
   Числа попадают в `metrics/tokenize.json` и в таблицу отчёта.
2. `max_seq_len` выбран по хвосту распределения длин train:

| перцентиль | p50 | p90 | p95 | p98 | p99 | p99.5 | max |
|---|---:|---:|---:|---:|---:|---:|---:|
| токенов | 268 | 405 | 458 | 501 | 543 | 594 | 979 |

| `max_seq_len` | обрезано train | обрезано val |
|---|---:|---:|
| 232 | 69.5% | 71.0% |
| 448 | 5.7% | 7.1% |
| **512** | **1.7%** | **2.1%** |
| 576 | 0.6% | 0.6% |
| 640 | 0.3% | 0.1% |

Выбрано 512 — между p98 и p99, ниже порога 5% на обоих сплитах с запасом.
576 и выше срезают меньше, но при динамическом паддинге за это платит самый
длинный батч: внимание квадратично по длине, на 576 оно дороже, чем на 512,
в (576/512)² ≈ 1.27 раза. Пик памяти на самом длинном батче — главный риск
ДЗ 5 на Mac с 24 ГБ.

### Чем подтверждается

При `max_seq_len` 512:

- обрезано 478 примеров train (1.7%) и 75 val (2.1%) при пороге 5%;
- выброшен 1 пример train, у которого промпт длиннее 512 токенов;
- до лосса доходит 3 670 761 из 3 702 438 токенов ответа, теряется 0.86%.

Проверка 5: `train 1.7%, val 2.1% при пороге 5.0%`.

---

## Что изменено помимо дефектов

- `model.name`: `Qwen/Qwen3-0.6B` → `Qwen/Qwen3-1.7B` — модель, выбранная
  в ДЗ 1 под эту машину. Токенизаторы у обеих моделей одинаковые: словарь из
  151 669 записей, шаблон чата, eos и pad совпадают. На 300 диалогах val
  совпали и строки, и `input_ids`. Тензоры от замены не изменились.
- `train_estimate.tokens_per_sec`: 52 → 43.85 — медиана моего замера генерации
  из ДЗ 1 (M4 Pro, MPS, Qwen3-1.7B, bfloat16). 52 ток/с — замер другой машины.
- Прогноз времени: 8 119 427 токенов × 3 эпохи / 43.85 ток/с ≈ 154 ч.
  Это оптимистичная граница: обучение медленнее генерации в 2–3 раза, то есть
  реально 300–460 ч. На полном train три эпохи на этой машине нереальны.
  В ДЗ 5 придётся брать подвыборку train или одну эпоху, и прогноз сверится
  с живым замером.
- Packing (`packing.enabled: true`, код не менялся): 28 878 примеров укладываются
  в 16 482 бина по 512 токенов, заполнение 96%, до 3 примеров в бине. Шагов
  оптимизатора при batch_size 8: 3 610 → 2 061 (−43%). Рядом с бинами
  сохранены `seq_lens` для блочно-диагональной маски.
- `metrics/tokenize.json` и `docs/tokenize_report.md` убраны из `.gitignore`:
  это сдаваемые артефакты. Метрики объявлены в `dvc.yaml` с `cache: false`,
  то есть по замыслу хранятся в git.
- `tests/check.sh` не менялся.

## Стадия в DVC-пайплайне после `split`

Проект ДЗ 4 — отдельный DVC-проект (`dvc init --subdir`), вход в него приходит
из DVC-проекта ДЗ 3 через `dvc import`. Сам ДЗ 3 не менялся.

```
+-----------------+     +---------------+
| train.jsonl.dvc |     | val.jsonl.dvc |      dvc import: выход split ДЗ 3
+-----------------+     +---------------+
              \             /
               +----------+
               | tokenize |                    dvc.yaml
               +----------+
```

- `train.jsonl.dvc` и `val.jsonl.dvc` хранят путь к выходу `split` ДЗ 3,
  коммит `09f8427` (merge сданного ДЗ 3, датасет v2) и md5:
  `c93337791c93106a3d8be91df744a807` и `7f709dd22f39bec40c88f4500d065460`.
  Они совпадают с `outs` стадии `split` в `dvc.lock` ДЗ 3 на этом коммите.
- Коммит указан явно (`rev`), а не «текущий HEAD»: ДЗ 3 и ДЗ 4 лежат в одном
  git-репозитории, и без закрепления любой новый коммит, даже не трогающий
  ДЗ 3, `dvc status` показывал как `update available`.
- Указатели лежат в корне проекта, а не рядом с данными: DVC не создаёт
  `.dvc`-файл в каталоге, который игнорирует git, а `data/` в `.gitignore`
  целиком — этого требует проверка 9.
- `dvc.lock` фиксирует md5 входа, кода, секций `params.yaml` и выходов
  `tokenize`: `data/tokenized` в `outs`, `metrics/tokenize.json` в `metrics`.
- Remote — локальный `dvc_remote/` (в `.gitignore`), как в ДЗ 3. `dvc push`
  выгрузил 5 файлов, 162 МБ.
- Стадия детерминирована: после `make tokenize` (его вызывает `check.sh`)
  `dvc status` пишет `Data and pipelines are up to date`. После удаления
  `data/` `dvc checkout` восстановил вход и тензоры с теми же md5.

Команды: `make update REV=<коммит>` переводит вход на другую версию выхода `split`,
`make repro` пересчитывает `tokenize`, если изменились вход, код или параметры,
`make push` выгружает кэш.
